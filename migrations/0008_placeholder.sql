-- Migration 0008: Placeholder (intentionally empty)
--
-- This number was skipped during earlier development. This no-op placeholder
-- keeps the migration sequence contiguous and 4-digit zero-padded so that
-- tooling that relies on sequential ordering does not encounter gaps.
-- Safe to apply: it performs no schema changes.
SELECT 1;
